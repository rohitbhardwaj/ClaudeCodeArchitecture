# Individual Reflection Worksheet

## Participant Name

```text

```

## Team / Product Area

```text

```

## 1. Where are we already doing vibe coding?

Think about places where work is delegated with vague intent, weak constraints, or limited review.

Examples:

```text
large refactors
test generation without reviewing assertions
API cleanup
documentation that becomes assumed truth
legacy modernization
security-sensitive code changes
```

Your answer:

```text







```

## 2. Which tasks are safe for Claude Code today?

Think about low-risk, reviewable, or read-only tasks.

Examples:

```text
explain code
summarize a module
draft documentation
generate candidate unit tests
identify risk hotspots
summarize a PR
draft an ADR
```

Your answer:

```text







```

## 3. Which tasks require architect approval?

Think about work with high blast radius.

Examples:

```text
authentication
authorization
payments
pricing
tax
refunds
public APIs
database migrations
large refactors
CI/CD
production configuration
```

Your answer:

```text







```

## 4. Which repeated review comment should become written guidance?

Think about comments you or senior engineers repeat often.

Examples:

```text
Keep business logic out of controllers.
Preserve public API error codes.
Do not duplicate validation rules.
Add failure-path tests.
Do not log customer identifiers.
Use existing service boundaries.
```

Your answer:

```text







```

## 5. What is one rule we should add to CLAUDE.md?

Write one clear, actionable rule.

A strong rule should say:

```text
what Claude should do
what Claude should not do
when Claude must ask first
what tests or evidence are required
```

Your rule:

```text







```
