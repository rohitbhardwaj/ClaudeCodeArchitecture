openapi: 3.0.0
info:
  title: Checkout API
  version: 1.0.0

components:
  schemas:
    CheckoutResponse:
      type: object
      properties:
        errorCode:
          type: string
          enum:
            - INVALID_REQUEST
            - CHECKOUT_REJECTED
            - OK

# Public API behavior should be preserved.
