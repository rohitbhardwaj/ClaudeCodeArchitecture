# Rollback Runbook

## Required for Production-Impacting Changes

Every production-impacting PR must include:

```text
revert strategy
feature flag or kill switch if available
data/schema impact
validation after rollback
owner and decision maker
```
