openapi: 3.0.0
info:
  title: Checkout API
  version: 1.0.0

components:
  schemas:
    CheckoutResponse:
      type: object
      properties:
        errorCode:
          type: string
          enum:
            - INVALID_COUPON
            - PAYMENT_DECLINED
            - OK

# Public API error-code changes require API owner approval.
